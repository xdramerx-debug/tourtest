# USER_FLOWS.md — источник правды №11. Ключевые сценарии

> Статус: **0.2 (v2)** · Дата: 2026-09-18
> Каждый сценарий = основа приёмочного теста. Шаги ссылаются на роуты IA.md; бюджеты времени — на UX-критерии и NFR.md.

## F0+v2. Пользователь переключает шаблон на лету (v2)

**Каналы:** UI-пикер (topnav/tabbar), Alt+T / Alt+1..5, `?theme=N`, настройки профиля.
**Ожидание:** мгновенная смена темы; без перезагрузки; введённый счёт/открытые фильтры/текущий роут не теряются; выбор пишется в localStorage и в профиль клуба; URL обновляется на `?theme=N` (шарится); системная тема — приоритет дефолта; без мигания и сдвига макета (no FOUC/no CLS).
**Приёмочный e2e:** на `/play` ввести счёт → Alt+T ×2 → Assert score input & board value без изменений → перейти на board по deep-link `?theme=4` → тема 4 применена с первого кадра.

## F1. Игрок вводит счёт (ядро продукта)

**Предусловие:** турнир live, игрок присоединился (F5), на 7-й лунке, группа из 3, связь нестабильна.
1. Игрок открывает приложение → на `/` жмёт «Моя карточка» (≤1 клик). Восстановление сессии до карты ≤1 с из кэша SW.
2. Карта открыта на **текущей лунке** (авто-вычисление Thru+1): крупно «Лунка 7 · Пар 4 · 412 м», звёздочка гандикап-удара, если есть.
3. Ввод: тап по «5» на Numpad ИЛИ чип «Par+1» → цифра рендерится **мгновенно** (≤100 мс), оптимистично; итог To par обновляется тут же.
4. Авто-переход на лунку 8 ( или ручной свайп). Оповещение «рыбак» — тактильное/визуальное микроподтверждение (по дизайну), без блокирующих тостов.
5. Синхронизация фоновая: индикатор показывает «2 в очереди» → через ≤30 с при связи — «онлайн». На ввод не влияет.
6. (Опция) Маркер-режим: свайп вверх по имени → карточка партнёра, ввод за него; «за кого» маркировано на каждой ячейке.

**Приёмка (UX-критерии):** ≤5 сек на лунку одной рукой у 5/5 тестовых пользователей без обучения; счёт не теряется при сворачивании/перезагрузке вкладки; ИТОГ сохраняется в IndexedDB до подтверждения сервера.

## F2. Зритель смотрит live-лидерборд

**Предусловие:** зритель у грина 12-й, получил ссылку/QR.
1. Переходит по ссылке `/t/[tid]/board` (или с `/` → карточка турнира → «Live», ≤2 кликов).
2. Видит доску: Pos/Player/To par/Thru, секция «за кем следить» (избранное из query `?player=`).
3. Событие «бёрди лидера на 14-й» приходит на сервер → зритель видит дельту **≤2 с (p95)**: строка мягко подсвечивается (стиль дизайна), пересортировка только вне зоны касания.
4. Тап по строке → детали hole-by-hole; возврат одним жестом назад (scroll позиция сохранена).
5. Потеря сети: баннер «офлайн, данные на 14:05»; восстановление — автодокачка без перезагрузки.

**Приёмка:** 5/5 находят лидерборд за ≤2 клика; свежесть p95 ≤2 с; 100+ обновлений/мин без деградации (RT-тест).

## F3. Админ создаёт турнир (≤5 мин)

1. `/admin` → «Новый турнир» → мастер, 5 шагов:
2. **База:** название, дата(ы), поле (из настроенных), стартовая лунка/ти.
3. **Формат:** выбор из FORMATS.md (доступные по варианту сборки), allowance, cap, зачёты гросс/нетто.
4. **Участники:** CSV-импорт (имя;HI;ти) с валидацией и предпросмотром ИЛИ ручное добавление; 144 игрока импорт ≤30 с.
5. **Флайты/группы** (B/C): авто-разбивка по HI (равные флайты, границы по разрывам HI), ручная правка drag'ом.
6. **Публикация:** код турнира 6 симв. + QR/PDF-заготовка; статус `registration` → `live` по кнопке/расписанию.

**Приёмка:** ≤5 мин с готовыми данными; каждый шаг валидируется заранее (нет «ошибки в конце»); турнир виден зрителям без перезагрузки.

## F4. Судья правит счёт

1. `/admin/tournaments/[tid]` → вкладка «Счёт» → поиск игрока → сетка лунок.
2. Тап по ячейке → «12 → 8, причина: неверно записан штраф» (причина обязательна, свободный текст или код).
3. Изменение применяется **с приоритетом роли судьи** (RULES-конфликт: судья > маркер > игрок), распространяется всем клиентам ≤2 с; в аудите — автор, старое/новое значение, время.
4. Видны «конфликтные» ячейки (разные авторы, окно ±5 с) — судья разрешает одним выбором; отклонённая версия остаётся в аудите.

**Приёмка:** идемпотентно (повтор клика/ретрая не дублирует); зрители видят исправление ≤2 с; аудит-запись полна.

## F5. Игрок регистрируется/присоединяется

1. По QR → `/join/[code]` (или ввод кода вручную, 6 симв., большие клавиши).
2. Анкета: имя, HI (слайдер/число, подсказка «не знаете — оставьте 18.0»), набор ти, согласия (правила, фото — галочка). ≤2 мин.
3. Создание сессии (токен 24 ч) → редирект на `?next=` (карту) или лобби.
4. Если игрок уже в базе клуба — автоподстановка по имени (первичный ключ — созданный админом entry; дубликаты имён помечаются админу).

**Приёмка:** без паролей; работа с 375px в одну руку; ошибки кода — понятные («код не найден / турнир завершён»).

## F6. Клуб настраивает поле (≤10 мин)

1. `/admin/courses/[cid]` → спецификация: число лунок (9/18/27/36), для каждой лунки: пар, SI, длины по наборам ти.
2. Наборы ти: championship/men's/women's/senior/junior — каждый: CR, Slope, длина Y/M; быстрый пресет «клонировать из men's».
3. Валидация: SI уникальны 1..18 (на девятку), пар 3..5, суммы OUT/IN — подсчёт автоматом; сохранение ≤1 с, поле готово к турнирам.
4. Проверка: «превью карточки» игрока с HI 15.4, men's ти → верные удары по SI.

**Приёмка:** ≤10 мин с данными измерителя; без правки кода; изменение поля на живом турнире запрещено (только новая редакция курса; иначе предупреждение + рекомендация).

---

## Edge-сценарии (обязательные тесты)

| ID | Сценарий | Ожидание |
|---|---|---|
| E1 | Потеря сети на 9-й лунке, раунд доигран офлайн (9 лунок) | Все 9 введены, очередь 9–36 действий, sync ≤30 с после связи, ноль потерь |
| E2 | Одновременный ввод с 4 устройств (игрок+маркер+судья+игрок повторно) за одну лунку | Итог по LWW+роль; все версии в аудите; гонок в UI нет |
| E3 | Конфликт правок в окне ±5 с: маркер «5», судья «4» | Побеждает судья; маркер видит причину в истории ячейки |
| E4 | Игра в темноте: After Dark-сборка, OLED, battery-saver | Читаемость, glow отключён, SSE пауза в фоне, докачка по возврату |
| E5 | DQ/WD/DNS посреди раунда | Плашки в лидерборде ≤2 с; счёт сохранён; зачёты пересчитаны |
| E6 | Ничья в stroke → countback до последней лунки → T5 | Корректные T-позиции, подсказка тай-брейка в деталях |
| E7 | Сворачивание браузера/блокировка телефона на 2 ч | Возврат → карта на месте, счёт цел, SSE резюм по Last-Event-ID |
| E8 | 100+ обновлений/мин при 200+ зрителях | p95 свежести ≤2 с, клиентский FPS не ниже 30 на Moto G4-class |
| E9 | Один игрок в двух турнирах в один день | Сессии/карты разделены, «Моя карточка» ведёт на ближайший live |
