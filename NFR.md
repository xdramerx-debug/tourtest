# NFR.md — источник правды №5. Нефункциональные требования (SLA/SLO/бюджеты)

> Статус: **0.2 (v2)** · Дата: 2026-09-18
> Каждое требование имеет метод проверки. v2-приоритет: единое приложение + 5 тем в одном бандле.

## 0. Бюджеты v2 (единый бандл 5 тем)

| Метрика | Бюджет v2 | Как достигается |
|---|---|---|
| JS-бандл initial | ≤ рост v1-варианта-C **+12%** (весь функционал v1-C уже включал весь продукт; дельта — ThemeProvider и CSS всех тем) | tree-shaking по маршрутам/теме запрещён: JS один, а CSS тем и шрифты расселены |
| CSS | сумма `base + theme-1..5 + ds` **≤ 60 КБ gzip** (проверяется `check:bundle`) | CSS-токены компактны (5×наборы переменных), ds.css общая одна |
| Шрифты | ≤ 1 семейство на demand при первом входе в тему (woff2 subsets, font-display: swap) | лениво браузером: только при фактическом использовании `font-family` |
| Переключение темы | **P95 ≤ 100 мс**, без сетевого зависания (данные не заново не грузятся) | только data-attrs + CSS-selector flip; fonts swap по мере готовности |
| CLS при переключении | 0 | текстовые метрики display/numeric выровнены у всех тем |
| Остальные метрики | как в §1–§7 (объёмы, WVC, a11y, offline) | без изменений |

## 1. Целевые объёмы (ёмкость)

| Параметр | Цель | Проверка |
|---|---|---|
| Игроков в турнире | 144 (4 флайта × 36), 18 лунок, до 3 раундов | Нагрузочный скрипт + фикстура `tournament-144` |
| Одновременных авторов ввода | 72 группы × 2 устройства = 144 | Контрактный тест sync |
| Зрителей на SSE | 500 одновременных подключений на 1 реплику сервера | Нагрузочный скрипт `load-sse` |
| Обновления счёта | 100+/мин sustained, burst 300/мин | Нагрузочный скрипт |
| Offline-сессия | 4+ часа, очередь ≥2000 действий, локал-стейт ≤50 МБ | Offline e2e |

## 2. Производительность клиента (бюджеты)

Измерение: Lighthouse CI на Moto G4-class (4× CPU throttle, Fast 3G) ◆ + полевой p75 по web-vitals.

| Метрика | Бюджет | Область |
|---|---|---|
| LCP | ≤2.5 с (lab, главная/лидерборд/счётокарта) | ◆ |
| INP | ≤200 мс (ввод счёта, переключение лунок/игроков) | ◆ |
| CLS | ≤0.05 | ◆ |
| TTFB (статика CDN) | ≤800 мс p95 | ◆ |
| Начальный JS (gzip, критический путь игрока/зрителя) | ≤200 КБ | ◆ |
| CSS (gzip) | ≤45 КБ | ◆ |
| Ленивые чанки: admin | ≤250 КБ, графики/аналитика (C) | ≤200 КБ суммарно |
| SW precache | ≤3 МБ (шрифты включены) | ◆ |
| Шрифты | ≤2 начертания display + ≤2 text на дизайн, woff2 subset cyr+lat | ◆ |
| Рендер дельты лидерборда | ≤100 мс на 144 строки (без полного перерендера) | ◆ |
| Число запросов на первый экран | ≤12 | ◆ |
| Вес одной синхронизации (batch ≤50 действий) | ≤20 КБ типично / ≤64 КБ макс | ◆ |

## 3. Real-time и синхронизация (SLO)

| SLO | Цель | Проверка |
|---|---|---|
| Свежесть лидерборда | p95 ≤2 с от приёма события сервером до рендера у зрителя (4G); p50 ≤1 с | RT e2e: автор→сервер→зритель замер ◆ |
| Доставка из offline-очереди | p95 ≤30 с после восстановления связи (backoff 1→60 с, jitter 20%) | Offline e2e ◆ |
| Идемпотентность | Повторная отправка actionId не создаёт дубликатов (UNIQUE constraint + dedupe-лог) | Контрактный тест ◆ |
| Конфликты | LWW по clientTimestamp; при равенстве (окно ±5 с) — роль: судья > маркер > капитан > игрок; все ветки в аудите | Unit + контрактный тест ◆ |
| Восстановление потока | Резюм по Last-Event-ID без пропусков; gap → автоматический snapshot-fetch | Контрактный тест ◆ |
| Деградация | Без сети зритель видит «данные на HH:MM», ввод игрока работает полностью | Offline e2e ◆ |

## 4. Надёжность и данные

| Требование | Цель |
|---|---|
| RPO локального ввода | **0** — введённый счёт персистится в IndexedDB до подтверждения сервером ◆ |
| Долговечность очереди | Переживает reload/сворачивание/перезагрузку устройства ◆ |
| Аудит | Каждое изменение счёта: actionId, автор, роль, client/server timestamp, payload; append-only ◆ |
| Серверная доступность | 99.9% в окна турниров (single-node + бэкап SQLite каждые 15 мин + restore-процедура; RTO ≤1 ч) |
| Консистентность лидерборда | Materialized view пересчитывается транзакционно с записью события |

## 5. Доступность (WCAG 2.2 AA)

- Контраст: текст ≥4.5:1, крупный и цифры счёта ≥4.5:1 базово и **≥7:1 в режиме «солнце»** ◆.
- Тач-таргеты ≥48×48px (перчатки); WCAG-минимум 24px недопустим в полевых экранах ◆.
- Фокус видим всегда (токен `color.focus`), порядок фокуса логичен, skip-link на таблицах ◆.
- Клавиатура: полный ввод счёта без мыши; шорткаты админки задокументированы ◆.
- Screen reader: роли/aria-live у лидерборда (`aria-live="polite"`, throttling анонсов ≤1/5с), счётокарта объявляет «Лунка 7, пар 4, ваш счёт 5» ◆.
- Motion: `prefers-reduced-motion` уважается всеми пятью дизайнами ◆.
- Zoom 200% и text-spacing override без потери функциональности.
- Ошибки форм: текстом + иконкой, не только цветом ◆.
- Проверка: axe-core на 3 ключевых экранах каждой сборки ◆ (приоритетные — + ручной SR-прогон VoiceOver/NVDA, лог в PR).

## 6. Безопасность

- OWASP ASVS L1: валидация всех входов zod-схемами (shared `core`), параметризованные запросы, экранирование вывода React по умолчанию ◆.
- CSP: `default-src 'self'`, без `unsafe-inline` для скриптов (nonce-хэш), `connect-src` только API-домен; Security headers: HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy ◆.
- AuthN: игрок/маркер — код турнира (6 симв., rate-limit 10/мин/IP) + сессионный токен (24 ч); судья/админ — логин/пароль (Argon2id) + JWT access 12 ч. AuthZ — только серверная, проверки на каждом эндпоинте ◆.
- Rate limiting: мутации 60/мин/сессия, join 10/мин/IP, экспорт 10/час.
- Секреты — только env сервера; скан репо (gitleaks) в CI ◆.
- XSS/CSRF: токены в Authorization header (cookies не используются → CSRF неприменим), SSE read-only.
- Аудит-лог append-only; правки счёта требуют причины ◆.
- Приоритетные сборки: dependency audit (npm audit high=0) + security-чек в PR.

## 7. Приватность

- Минимум PII: имя, HI, клуб, опц. email/телефон админов. Зрительские ссылки не раскрывают контакты ◆.
- Без сторонних трекеров; web-vitals телеметрия — агрегированная, без идентификаторов личности ◆.
- Retention: сырая телеметрия 30 дней; результаты турниров — по решению клуба; удаление игрока по запросу (экспорт/стирание) — процедура в CONTRIBUTING.
- Комментарии/фото (вариант C): только внутри турнира, удаление автором и админом, премодерация foto — через явный аплоуд админом/игроком (бот-модерация out of scope).

## 8. Батарея и полевые условия

- Без wake-lock по умолчанию; SSE ставится на паузу через 5 мин в background (visibility API) с докачкой по возврату ◆.
- Никаких бесконечных CSS-анимаций на счётокарте в battery-saver ◆.
- Яркое солнце: режим `sun` на каждом дизайне (см. DESIGNS.md) ◆; эмуляция проверки: снимки + axe контраст ◤приоритетные◢.
- Перчатки: все полевые контролы ≥48px, без hover-only поведения ◆.
- Слабая связь: приложение полностью функционально на вводе при 0 связи; критические ассеты кэшированы SW ◆.

## 9. i18n / l10n

- Все строки — через i18n-слой; хардкод запрещён (lint-правило) ◆.
- Числа/даты/время — `Intl` с локалью клуба/пользователя; TZ турнира = TZ клуба, храним UTC ◆.
- Единицы: метры/ярды (и км/мили за пределами поля) — переключаемые в настройках клуба ◆.
- RTL: верификация снимками на всех 5 дизайнах (pseudo-locale + dir=rtl) ◆.
- Длина строк +30% (немецкий/финский тест pseudo-locale) — layout не ломается ◆.

## 10. Наблюдаемость

- web-vitals (LCP/INP/CLS/TTFB) по экрану и сборке → `POST /api/metrics` (sendBeacon), дашборд p75 в admin health ◆.
- Клиентские ошибки: window.onerror/unhandledrejection → `/api/errors` (без PII, sampling 100% на турнире) ◆.
- Sync-метрики: размер очереди, возраст старейшего действия, конфликты/мин, RTT SSE — в админ-дашборде ◆.
- Структурные логи сервера (pino JSON), request-id сквозной ◆.

## 11. Обязательный минимум ◆ vs полный NFR

Неприоритетные сборки могут отклоняться от: нагрузочных 500 зрителей (достаточно 100), полевого теста battery-saver, ручного SR-прогона, security-чека — **только с явной таблицей отклонений в PR** (шаблон в REVIEW.md). Всё помеченное ◆ — обязательно для всех 15.
